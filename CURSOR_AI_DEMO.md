# Cursor AI 使用展示文档

## 📋 目录

1. [核心优点](#核心优点)
2. [Cursor 开发模式](#cursor-开发模式)
3. [实际演示场景](#实际演示场景)
4. [对比其他工具](#对比其他工具)
5. [展示流程建议](#展示流程建议)
6. [隐私模式说明](#隐私模式说明)
7. [常见问题](#常见问题)
8. [展示大纲](#展示大纲)
---
## 🎯 核心优点
### 1. **代码理解能力**
- ✅ 快速理解大型代码库，无需人工逐行阅读
- ✅ 智能分析代码结构和依赖关系
- ✅ 准确解释复杂代码逻辑

**演示方式**：打开一个复杂文件，让我解释其功能和逻辑

### 2. **智能代码搜索**
- ✅ 语义搜索：用自然语言描述需求，无需记住精确函数名
- ✅ 跨文件搜索：理解上下文，找到相关代码
- ✅ 代码库级别的理解

**演示方式**：使用语义搜索功能查找特定功能实现

### 3. **上下文感知**
- ✅ 理解整个文件和项目结构
- ✅ 修改代码时考虑相关文件的影响
- ✅ 减少因缺乏上下文导致的错误

**演示方式**：修改代码时展示我能理解整个文件和相关文件

### 4. **快速重构**
- ✅ 智能提取代码到服务/工具函数
- ✅ 保持代码风格一致性
- ✅ 改善代码组织和可维护性

**演示方式**：展示从项目中提取的真实重构案例（如 `calendar-config.service.ts`）

### 5. **实时交互**
- ✅ 即时问答，无需切换工具
- ✅ 支持追问和澄清
- ✅ 多种交互模式（Chat、Composer、Agent、Ask、Plan）

---

## 🔧 Cursor 开发模式

Cursor 提供了多种开发模式，以适应不同的任务需求：

### 1. **Agent 模式**（默认模式）

**功能特点**：
- ✅ 可以自主探索代码库
- ✅ 能够编辑多个文件
- ✅ 可以执行终端命令
- ✅ 能够修复错误
- ✅ 启用所有可用工具（搜索、编辑、终端等）

**适用场景**：
- 复杂功能开发
- 代码重构
- 多文件修改
- 需要执行命令的任务

**使用方式**：
- 在 Composer 中直接输入需求，Agent 会自动执行

**示例**：
```
"将这个组件的所有交互逻辑抽取到服务中"
→ Agent 会自动创建服务文件、修改组件、更新导入等
```

---

### 2. **Ask 模式**（只读模式）

**功能特点**：
- ✅ 在代码库中搜索并提供答案
- ✅ **不会进行任何代码修改**
- ✅ 主要用于学习和探索
- ✅ 只使用搜索工具

**适用场景**：
- 理解现有代码
- 查找功能实现
- 规划解决方案
- 学习代码库结构

**使用方式**：
- 在 Composer 中选择 "Ask" 模式
- 或者在提问前明确说明："只查看，不要修改"

**示例**：
```
"这个项目是如何处理用户认证的？"
→ Ask 模式会搜索相关代码并解释，但不会修改任何文件
```

---

### 3. **Plan 模式**（规划模式）

**功能特点**：
- ✅ 通过结构化的待办列表管理任务
- ✅ 支持消息队列和前瞻性规划
- ✅ 使长周期任务更容易理解与跟踪
- ✅ 可以分步骤执行复杂任务

**适用场景**：
- 大型重构任务
- 多步骤功能开发
- 需要跟踪进度的复杂项目
- 团队协作规划

**使用方式**：
- 在 Composer 中选择 "Plan" 模式
- 或者直接说："为这个任务创建一个计划"

**示例**：
```
"重构整个认证系统"
→ Plan 模式会创建待办列表：
  1. 分析现有认证逻辑
  2. 设计新的服务结构
  3. 逐步迁移功能
  4. 更新测试用例
  5. 验证功能完整性
```

---

### 4. **自定义模式（Custom）**

**功能特点**：
- ✅ 可以自定义工具组合
- ✅ 可以设置特定的指令
- ✅ 根据个人工作流程定制
- ✅ 可以创建自己的 "Plan" 或其他模式

**适用场景**：
- 特定工作流程需求
- 团队标准化操作
- 特定技术栈的定制

**使用方式**：
- 在 Cursor 设置中创建自定义模式
- 选择需要的工具和指令模板

---

### 模式对比表

| 特性 | Agent | Ask | Plan | Custom |
|------|-------|-----|------|--------|
| **修改代码** | ✅ 可以 | ❌ 只读 | ✅ 可以 | 可配置 |
| **执行命令** | ✅ 可以 | ❌ 不可以 | ✅ 可以 | 可配置 |
| **搜索代码** | ✅ 可以 | ✅ 可以 | ✅ 可以 | 可配置 |
| **任务规划** | ⚠️ 自动 | ❌ 不规划 | ✅ 结构化 | 可配置 |
| **适用场景** | 复杂任务 | 学习探索 | 大型项目 | 定制需求 |

---

### 模式选择建议

**选择 Agent 模式**：
- ✅ 需要实现新功能
- ✅ 需要重构代码
- ✅ 需要修改多个文件
- ✅ 需要执行命令或测试

**选择 Ask 模式**：
- ✅ 只是想理解代码
- ✅ 查找功能实现
- ✅ 规划解决方案
- ✅ 学习代码库

**选择 Plan 模式**：
- ✅ 大型重构任务
- ✅ 多步骤功能开发
- ✅ 需要跟踪进度
- ✅ 团队协作

**选择 Custom 模式**：
- ✅ 有特定工作流程
- ✅ 需要标准化操作
- ✅ 特定技术栈需求

---

### 关于 "Background" 模式

**注意**：Cursor 官方文档中**没有名为 "Background" 的独立模式**。

可能的情况：
1. **后台执行**：Agent 模式在执行任务时会在后台运行，但这不是一个独立模式
2. **自定义模式**：可能是某个自定义模式的名称
3. **记忆混淆**：可能与其他工具的功能混淆

如果需要后台执行任务，Agent 模式已经支持在后台处理复杂任务。

---

## 🎬 实际演示场景

### 场景 1：代码理解（3-5 分钟）

**演示步骤**：
1. 打开 `calendar-data-handle.ts`
2. 提问："这个文件是做什么的？"
3. 展示 AI 的回答（解释文件结构和功能）
4. 提问："EventInput 和 any 的区别是什么？"
5. 展示详细的类型解释

**展示要点**：
- 快速理解复杂代码
- 准确解释技术概念
- 提供实际代码示例

### 场景 2：代码重构（5-7 分钟）

**演示步骤**：
1. 展示重构前的代码（`calendar-modal.component.ts` 中的复杂逻辑）
2. 提问："能否将这些交互逻辑抽取到服务中？"
3. 展示创建的新服务（`calendar-config.service.ts`）
4. 解释重构的好处和设计模式

**展示要点**：
- 代码组织改进
- 职责分离
- 可维护性提升

**实际案例**：
- 从 `calendar-modal.component.ts` 抽取逻辑到 `calendar-config.service.ts`
- 使用 `CalendarComponentState` 接口实现组件与服务的解耦

### 场景 3：问题排查（5 分钟）

**演示步骤**：
1. 展示一个实际问题（如 CI/CD 权限问题）
2. 提问："这个错误怎么解决？"
3. 展示分析和解决方案
4. 展示修复后的代码

**展示要点**：
- 快速定位问题
- 提供解决方案
- 解释问题原因

**实际案例**：
- GitHub Actions 中 `n 24.7.0` 权限问题
- 解决方案：直接使用 `yarn ng build` 而不是调用包含 `n` 的脚本

### 场景 4：新功能开发（5 分钟）

**演示步骤**：
1. 描述一个简单需求（自然语言）
2. 让 AI 生成代码
3. 检查和完善代码
4. 展示代码质量

**展示要点**：
- 快速原型开发
- 代码质量
- 符合项目规范

### 场景 5：类型定义理解（3-5 分钟）

**演示步骤**：
1. 打开一个复杂的类型定义（如 `EventInput`）
2. 提问："这个类型定义的作用是什么？"
3. 展示详细的解释和示例
4. 回答相关问题（可选/必选属性等）

**展示要点**：
- 理解复杂类型系统
- 解释类型约束
- 提供使用示例

---

## 📊 对比其他工具

| 特性 | Cursor AI | GitHub Copilot | ChatGPT | 传统搜索 |
|------|-----------|----------------|---------|----------|
| **代码理解** | ✅ 强 | ⚠️ 中等 | ✅ 强 | ❌ 无 |
| **上下文感知** | ✅ 完整项目 | ⚠️ 有限 | ⚠️ 有限 | ❌ 无 |
| **实时交互** | ✅ 是 | ✅ 是 | ⚠️ 需复制粘贴 | ❌ 否 |
| **代码搜索** | ✅ 语义搜索 | ❌ 无 | ❌ 无 | ⚠️ 关键词搜索 |
| **重构能力** | ✅ 强 | ⚠️ 中等 | ✅ 强 | ❌ 无 |
| **项目级理解** | ✅ 完整 | ⚠️ 有限 | ⚠️ 有限 | ❌ 无 |
| **隐私保护** | ✅ 隐私模式 | ⚠️ 一般 | ⚠️ 一般 | ✅ 完全本地 |

---

## 🎤 展示流程建议

### 30 分钟版本

#### 1. 开场介绍（2 分钟）
```
"大家好，今天给大家展示一下 Cursor AI 的使用方式。
这是一个基于 AI 的代码助手，可以帮助我们更高效地开发。
我会从几个实际场景来展示它的能力。"
```

#### 2. 核心功能演示（15 分钟）
- **代码理解**（3 分钟）
  - 打开复杂文件
  - 提问并展示回答
  - 解释代码逻辑

- **代码搜索**（3 分钟）
  - 使用语义搜索
  - 展示跨文件理解能力

- **代码重构**（5 分钟）
  - 展示重构案例
  - 解释重构过程
  - 展示重构效果

- **问题排查**（4 分钟）
  - 展示实际问题
  - 展示解决方案
  - 解释修复过程

#### 3. 实际项目案例（8 分钟）
- 展示项目中的实际使用案例
- 展示节省的时间
- 展示代码质量改进

#### 4. 对比和优势（3 分钟）
- 与其他工具对比
- 核心优势总结
- 适用场景说明

#### 5. Q&A（5 分钟）
- 回答同事问题
- 使用建议
- 最佳实践分享

---

## 🔒 隐私模式说明

### Cursor 的隐私选项

#### 1. **隐私模式（Privacy Mode）**
- ✅ 启用后，代码数据不会以明文形式存储在服务器
- ✅ 请求结束后，明文代码会被删除
- ✅ 适合处理敏感代码

**如何启用**：
1. 打开 Cursor 设置（快捷键 `Cmd/Ctrl + ,`）
2. 找到 "Privacy" 或"隐私"选项
3. 启用隐私模式

#### 1.1. **使用本地模型（完全本地化）**

如果您需要完全本地化的方案，可以使用 Ollama 运行本地模型：

**步骤 1：安装 Ollama**

```bash
# macOS (使用 Homebrew)
brew install ollama

# 或者访问官网下载
# https://ollama.com/
```

**步骤 2：下载本地模型**

```bash
# 下载 CodeLlama 模型（推荐用于代码）
ollama pull codellama

# 或者下载其他模型
ollama pull llama2
ollama pull mistral
```

**步骤 3：在 Cursor 中配置本地模型**

**方法一：通过设置界面配置**

1. **打开 Cursor 设置**
   - 点击右上角的齿轮图标（⚙️）
   - 或使用快捷键：`Cmd + ,`（macOS）或 `Ctrl + ,`（Windows/Linux）

2. **导航到模型设置**
   - 在设置面板左侧，找到并点击 "Models" 或 "模型" 选项卡
   - 或者搜索 "Ollama" 或 "Model"

3. **配置 Ollama 连接**
   - 找到 "Ollama Base URL" 或 "Ollama Server URL" 设置项
   - 输入：`http://127.0.0.1:11434`
   - 确保没有尾随斜杠，不要使用 `https://`

4. **选择模型**
   - 在 "Model" 或 "模型" 下拉菜单中
   - 选择 "Ollama" 或直接输入模型名称，如：`codellama:latest`
   - 不要选择 "Auto" 或 "Default"

5. **验证连接**
   - 如果设置中有 "Test Connection" 或 "验证" 按钮，点击测试
   - 确保显示连接成功

6. **保存设置**
   - 点击 "Save" 或 "保存" 按钮
   - 完全退出并重新启动 Cursor 以应用设置

**方法二：通过设置 JSON 文件配置（如果界面找不到设置项）**

如果设置界面中找不到 Base URL 配置项，可以通过直接编辑 JSON 配置文件：

1. **打开设置 JSON 文件**
   - 在 Cursor 中按 `Cmd/Ctrl + Shift + P` 打开命令面板
   - 输入 "Preferences: Open User Settings (JSON)"
   - 选择该选项打开设置 JSON 文件
   
   **或者直接打开文件**：
   - macOS: `~/Library/Application Support/Cursor/User/settings.json`
   - Windows: `%APPDATA%\Cursor\User\settings.json`
   - Linux: `~/.config/Cursor/User/settings.json`

2. **添加配置**
   在 JSON 文件中添加以下配置（注意：不同版本的 Cursor 配置项名称可能不同，尝试以下所有可能的配置）：
   
   ```json
   {
     // 方法 1: 尝试这些配置项
     "cursor.ai.model": "ollama",
     "cursor.ai.ollama.baseUrl": "http://127.0.0.1:11434",
     "cursor.ai.ollama.model": "codellama:latest",
     
     // 方法 2: 或者尝试这些配置项
     "cursor.chat.model": "ollama",
     "cursor.chat.ollama.baseUrl": "http://127.0.0.1:11434",
     "cursor.chat.ollama.model": "codellama:latest",
     
     // 方法 3: 或者尝试自定义 API 配置（OpenAI 兼容格式）
     "cursor.ai.customApiUrl": "http://127.0.0.1:11434/v1",
     "cursor.ai.customApiKey": "",
     "cursor.ai.customModel": "codellama:latest"
   }
   ```

3. **保存并重启**
   - 保存 JSON 文件（确保 JSON 格式正确，没有语法错误）
   - 完全退出 Cursor（`Cmd + Q` 或 `Ctrl + Q`）
   - 重新启动 Cursor

**方法三：通过自定义 API 配置（如果上述方法都不行）**

某些版本的 Cursor 可能需要通过"自定义 API"的方式配置 Ollama：

1. **打开 Cursor 设置**（`Cmd + ,`）
2. **找到 "AI" 或 "Features" 设置**
3. **查找 "Custom API" 或 "自定义 API" 选项**
4. **配置自定义 API**：
   - **API Type**: 选择 "OpenAI Compatible" 或 "OpenAI 兼容"
   - **API URL**: 输入 `http://127.0.0.1:11434/v1`（注意：需要 `/v1` 后缀）
   - **API Key**: 留空（Ollama 不需要密钥）
   - **Model ID**: 输入 `codellama:latest`
5. **保存并重启 Cursor**

**步骤 4：启用隐私模式**

1. 在 Cursor 设置的 "AI 功能" 部分
2. 启用 "隐私模式" 选项
3. 确认使用本地模型后，所有处理都在本地进行

**注意事项**：
- ⚠️ 本地模型需要足够的硬件资源（建议至少 16GB RAM）
- ⚠️ 较大模型可能需要专用 GPU 以获得更好性能
- ⚠️ 本地模型的能力可能不如云端模型，但对于代码补全和简单问答通常足够
- ✅ 使用本地模型后，代码完全不会上传到云端

#### 1.2. **断网时无法使用本地模型的排查和解决**

如果已经配置了 Ollama 本地模型，但断网时仍然无法使用，可能是以下原因：

**问题 1：Cursor 仍在尝试连接云端进行验证**

**解决方案**：
1. 打开 Cursor 设置（`Cmd/Ctrl + ,`）
2. 找到 "Features" 或 "功能" 设置
3. 关闭 "Require network for AI features"（如果存在）
4. 确保 "Use local model" 或 "Ollama" 选项已启用

**问题 2：Cursor 设置中没有正确选择本地模型**

**解决方案**：
1. 打开 Cursor 设置（`Cmd/Ctrl + ,`）
2. 导航到 "AI" → "Model" 或 "模型" 设置
3. 在模型选择下拉菜单中，明确选择 "Ollama" 或 "codellama"
4. 不要选择 "Auto" 或 "Default"，必须明确选择本地模型
5. 检查 "Ollama Base URL" 设置：
   - 尝试 `http://localhost:11434`
   - 如果不行，尝试 `http://127.0.0.1:11434`（根据你的终端输出，Ollama 运行在 127.0.0.1）
   - 确保没有尾随斜杠

**重要提示**：从你的终端输出看，Ollama 服务运行在 `127.0.0.1:11434`，而不是 `localhost:11434`。虽然通常两者等价，但某些情况下可能有区别。建议在 Cursor 设置中明确使用 `http://127.0.0.1:11434`。

**问题 3：Ollama 服务未启动**

**检查方法**：
```bash
# 检查 Ollama 服务是否运行
ps aux | grep ollama | grep -v grep

# 检查 Ollama API 是否可访问
curl http://localhost:11434/api/tags

# 测试本地模型是否工作
ollama run codellama "test"
```

**如果服务未运行，启动方法**：
```bash
# macOS: Ollama 通常会自动启动，如果没有：
ollama serve

# 或者重启 Ollama
brew services restart ollama
```

**问题 4：Cursor 需要网络进行初始验证**

**解决方案**：
- 即使使用本地模型，Cursor 可能仍需要首次联网验证
- 建议在有网络时完成初始配置和验证
- 配置完成后，断网时应该可以正常使用本地模型

**问题 5：检查 Cursor 的日志**

**排查步骤**：
1. 在 Cursor 中打开开发者工具（`Cmd/Ctrl + Shift + I`）
2. 查看 Console 标签页的错误信息
3. 查看是否有网络连接相关的错误
4. 查看是否有 Ollama 连接相关的错误

**完整配置检查清单**：
- [ ] Ollama 已安装并运行
- [ ] 已下载本地模型（如 `codellama`）
- [ ] Cursor 设置中已选择 "Ollama" 模型
- [ ] Ollama Base URL 设置为 `http://localhost:11434`
- [ ] 隐私模式已启用
- [ ] 已测试 Ollama 在命令行中正常工作
- [ ] Cursor 已重启以应用设置

**问题 6：Cursor 版本可能不支持本地模型**

**检查方法**：
- 确保 Cursor 是最新版本
- 某些旧版本可能不完全支持 Ollama 集成
- 检查 Cursor 的更新日志，确认支持本地模型功能

**问题 7：Ollama Base URL 配置错误**

根据你的终端输出，Ollama 服务运行在 `127.0.0.1:11434`。在 Cursor 设置中：

**详细设置步骤**：

1. **打开设置**
   - 按 `Cmd + ,`（macOS）或 `Ctrl + ,`（Windows）
   - 或点击右上角齿轮图标 ⚙️

2. **找到模型设置**
   - 在左侧菜单找到 "Models" 或 "模型"
   - 或在搜索框输入 "Ollama"

3. **设置 Base URL**
   - 找到 "Ollama Base URL" 或 "Ollama Server URL" 输入框
   - 输入：`http://127.0.0.1:11434`
   - ⚠️ 注意：不要使用 `https://`，不要添加尾随斜杠 `/`

4. **选择模型**
   - 在模型下拉菜单选择 "Ollama"
   - 或直接输入：`codellama:latest`

5. **保存并重启**
   - 点击保存
   - 完全退出 Cursor（`Cmd + Q` 或 `Ctrl + Q`）
   - 重新启动 Cursor

**如果找不到设置项**：

某些版本的 Cursor 可能设置位置不同，尝试：
- 在设置中搜索 "Ollama"
- 在设置中搜索 "Base URL"
- 查看 "AI" 或 "Features" 选项卡
- 检查是否有 "Advanced" 或 "高级" 选项

**问题 8：Cursor 需要特定的模型名称格式**

某些版本的 Cursor 可能需要完整的模型名称：
- 尝试使用 `codellama:latest` 而不是 `codellama`
- 或者使用 `codellama:7b`（如果已下载）

**如果以上都正确但仍无法使用**：
1. 完全退出 Cursor（不是关闭窗口，而是退出应用）
2. 重新启动 Cursor
3. 在 Cursor 中打开开发者工具（`Cmd/Ctrl + Shift + I`）
4. 查看 Console 标签页，尝试使用 AI 功能，观察错误信息
5. 检查 Network 标签页，看是否有对 Ollama API 的请求
6. 如果看到 404 或连接错误，检查 URL 配置
7. 如果还是不行，可能需要检查 Cursor 的版本，确保支持本地模型功能
8. 考虑在 Cursor 的 GitHub Issues 或社区论坛中搜索类似问题

#### 2. **本地模型 vs 云端模型**

| 特性 | 本地模型（Ollama） | 云端模型 |
|------|------------------|---------|
| **隐私** | ✅ 完全本地，代码不上传 | ⚠️ 可能上传到云端 |
| **性能** | ⚠️ 取决于硬件配置 | ✅ 高性能 |
| **成本** | ✅ 免费 | ⚠️ 可能需要付费 |
| **离线使用** | ✅ 支持 | ❌ 需要网络 |
| **能力** | ⚠️ 相对有限 | ✅ 更强大 |

**选择建议**：
- 敏感代码项目 → 使用本地模型
- 需要高性能 → 使用云端模型
- 离线开发 → 使用本地模型
- 复杂任务 → 使用云端模型

#### 3. **Composer 模式 vs Chat 模式**

**Composer 模式**：
- 会读取整个项目上下文
- 可以跨文件进行代码分析和修改
- 适合大规模重构和功能开发

**Chat 模式**（普通聊天）：
- 基于当前打开的文件或选中的代码
- 上下文范围较小
- 适合快速问答和单个文件修改

#### 4. **本地代码索引**
- Cursor 主要在本地进行代码索引和分析
- 支持 `.cursorignore` 文件控制哪些文件被索引
- 可以排除敏感或不需要的文件

#### 5. **.cursorignore 文件配置**

在项目根目录创建 `.cursorignore`：

```bash
# 忽略敏感文件
.env
*.key
secrets/
private/

# 忽略依赖目录
node_modules/
dist/
build/

# 忽略特定文件类型
*.log
*.tmp
```

---

## ❓ 常见问题

### Q1: 会不会泄露代码？
**A**: 
- Cursor 提供隐私模式，代码不会明文存储
- 可以通过 `.cursorignore` 排除敏感文件
- **可以使用本地模型（Ollama）实现完全本地化**
- 请求结束后明文代码会被删除

**完全本地化方案**：
1. 安装 Ollama：`brew install ollama`（macOS）
2. 下载模型：`ollama pull codellama`
3. 在 Cursor 设置中选择本地模型
4. 启用隐私模式

这样代码完全不会上传到云端。

### Q2: 准确率如何？
**A**: 
- 对于代码理解、重构、问题排查准确率很高
- 对于新功能开发需要验证和测试
- 建议始终进行代码审查

### Q3: 学习成本高吗？
**A**: 
- 学习成本低，主要是学会如何提问
- 10-20 分钟即可上手
- 多使用几次就能熟练掌握

### Q4: 适合哪些场景？
**A**: 
- ✅ 代码理解和解释
- ✅ 代码重构
- ✅ 问题排查和调试
- ✅ 代码解释和文档生成
- ✅ 快速原型开发
- ⚠️ 复杂业务逻辑需要人工验证

### Q5: 哪些代码会被分析？
**A**: 
- 默认分析整个项目
- 可以通过 `.cursorignore` 排除特定文件
- 在 Chat 模式下，主要基于当前打开的文件
- Composer 模式会读取整个项目上下文

### Q6: 会不会影响代码质量？
**A**: 
- AI 生成的代码需要人工审查
- 建议始终进行代码审查和测试
- 可以提高代码质量，但不能完全替代人工审查

### Q7: 支持哪些编程语言？
**A**: 
- 支持主流编程语言（TypeScript、JavaScript、Python、Java、Go 等）
- 特别适合 TypeScript/JavaScript 项目
- 对 Angular、React、Vue 等框架支持良好

---

## 📝 展示大纲

### 演示脚本（30 分钟）

```
1. 开场介绍（2 分钟）
   - 什么是 Cursor AI
   - 为什么使用它
   - 今天要展示什么

2. 核心功能演示（15 分钟）
   - 代码理解（3 分钟）
     * 打开 calendar-data-handle.ts
     * 提问并展示回答
   - 代码搜索（3 分钟）
     * 使用语义搜索
     * 展示跨文件理解
   - 代码重构（5 分钟）
     * 展示重构案例
     * calendar-modal.component.ts → calendar-config.service.ts
   - 问题排查（4 分钟）
     * 展示实际问题
     * 展示解决方案

3. 实际项目案例（8 分钟）
   - 展示项目中的实际使用案例
   - 展示节省的时间
   - 展示代码质量改进

4. 对比和优势（3 分钟）
   - 与其他工具对比
   - 核心优势总结

5. Q&A（5 分钟）
   - 回答同事问题
   - 使用建议
```

---

## 🎯 展示技巧

### 1. 准备演示素材
- ✅ 选择 2-3 个实际项目中的案例
- ✅ 准备重构前后的代码对比
- ✅ 准备常见问题场景
- ✅ 录制演示视频作为备用

### 2. 互动演示
- ✅ 现场提问，展示实时回答
- ✅ 展示不同类型的提问方式
- ✅ 展示如何追问和澄清

### 3. 展示实际成果
- ✅ 展示从项目中提取的真实案例
- ✅ 展示节省的时间（如快速定位问题）
- ✅ 展示代码质量改进（如重构效果）

### 4. 强调隐私和安全
- ✅ 说明隐私模式
- ✅ 展示 `.cursorignore` 配置
- ✅ 解释代码安全措施

---

## 📚 实际项目案例

### 案例 1：代码重构
**问题**：`calendar-modal.component.ts` 中代码耦合严重，交互逻辑和数据处理混在一起

**解决方案**：
- 创建 `calendar-config.service.ts` 服务
- 使用 `CalendarComponentState` 接口实现解耦
- 将交互逻辑抽取到服务中

**效果**：
- 代码更清晰，职责分离
- 易于维护和测试
- 可复用性提高

### 案例 2：问题排查
**问题**：GitHub Actions 构建失败，`n 24.7.0` 命令权限错误

**解决方案**：
- 分析问题原因（CI 环境权限限制）
- 修改构建脚本，直接使用 `yarn ng build`
- 同时修复 GitLab CI 配置

**效果**：
- 快速定位问题
- 提供完整解决方案
- 同时修复类似问题

### 案例 3：类型理解
**问题**：不理解 `EventInput` 类型定义，不知道可选/必选属性

**解决方案**：
- 解释 `EventInput` 的类型定义
- 说明可选和必选属性的判断方式
- 提供实际使用示例

**效果**：
- 快速理解复杂类型
- 掌握类型系统使用
- 提高代码质量

---

## 🚀 使用建议

### 最佳实践

1. **明确提问**
   - 描述清楚需求
   - 提供必要的上下文
   - 必要时提供代码示例

2. **逐步验证**
   - 生成的代码需要验证
   - 进行测试和审查
   - 不要盲目接受所有建议

3. **利用上下文**
   - 打开相关文件
   - 使用 Composer 模式进行大规模重构
   - 让 AI 理解整个项目结构

4. **保护隐私**
   - 启用隐私模式（如需要）
   - 使用 `.cursorignore` 排除敏感文件
   - 谨慎处理敏感代码

5. **持续学习**
   - 多尝试不同的提问方式
   - 学习如何更好地利用 AI
   - 分享使用经验

---

## 📞 联系方式

如有问题或建议，欢迎交流讨论！

---

**文档版本**：v1.0  
**最后更新**：2024年

